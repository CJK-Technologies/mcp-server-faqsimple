# FAQsimple MCP Server Configuration

# Required: Your FAQsimple API key (starts with "fs.")
FAQSIMPLE_API_KEY=fs.your.api.key.here

# Optional: API base URL (defaults to https://api.faqsimple.com/v1)
FAQSIMPLE_API_BASE=https://api.faqsimple.com/v1

# Optional: Cache timeout in milliseconds (defaults to 300000 = 5 minutes)
CACHE_TIMEOUT=300000

# Optional: Rate limit delay in milliseconds (defaults to 1000 = 1 second)
RATE_LIMIT_DELAY=1000

# Optional: Log level for debugging
LOG_LEVEL=info